# 井関物流 受払管理システム コード改修計画書

## 📋 概要

本文書は、2025年4月18日付け「生産受払システム機能追加について」の改修要件に基づく、IbUkeharai受払管理システムのコード改修計画書です。

**改修対象**: 井関物流 受払管理システム (IbUkeharai)  
**改修要件**: 8項目（基本機能5項目 + 請求書フォーマット変更3項目）  
**作成日**: 2025年6月6日  
**対象ブランチ**: main-tokunou-20250603-001

---

## 🎯 改修要件一覧

### 基本機能改修（5項目）

1. **自動計算機能**: 単価×数量による金額自動計算
2. **入力促進機能**: 未入力項目の背景色変更
3. **金額フィールド保護**: 金額の手入力禁止
4. **単位項目追加**: 請求書用の単位フィールド新設
5. **単位入力必須化**: 単位未入力時のエラー表示

### 請求書フォーマット変更（3項目）

6. **列分離表示**: 数量と単位を縦線で分離した表示形式
7. **RDLC修正**: OutputSeikyuReportS.rdlcの列構造変更
8. **列幅最適化**: 数量列3.0cm、単位列0.8cm設定

---

## 🗂️ 対象ファイル一覧

### VB.NETソースファイル
- `IbUkeharai/IbUkeharai/RegisterSeikyuMasterForm.vb` - 請求マスター登録画面
- `IbUkeharai/IbUkeharai/OutPutSeikyuForm.vb` - 請求書出力画面
- `IbUkeharai/IbUkeharai/UcDataGridView.vb` - データグリッド制御
- `IbUkeharai/DitDataAccess/DataBases/SqlDataBase.vb` - データベースアクセス層

### レポートファイル
- `IbUkeharai/IbUkeharai/OutputSeikyuReportS.rdlc` - 請求書レポート定義

### データベーススキーマ
- `T_SEIKYU` テーブル - 単位フィールド追加

---

## 🔧 詳細実装計画

### 1. データベーススキーマ変更

#### 1.1 T_SEIKYUテーブル修正

**目的**: 要件4（単位項目追加）に対応するため、T_SEIKYUテーブルにTANIフィールドを追加

**DDL文**:
```sql
-- 単位フィールド追加
ALTER TABLE T_SEIKYU 
ADD TANI VARCHAR(10) NOT NULL DEFAULT 'その他';

-- CHECK制約追加（値の制限）
ALTER TABLE T_SEIKYU 
ADD CONSTRAINT CK_T_SEIKYU_TANI 
CHECK (TANI IN ('個', '本', '台', '式', 'kg', 'm', '㎡', 'その他'));

-- インデックス追加（検索性能向上）
CREATE NONCLUSTERED INDEX IX_T_SEIKYU_TANI ON T_SEIKYU (TANI);

-- 既存データの初期値設定
UPDATE T_SEIKYU SET TANI = 'その他' WHERE TANI IS NULL OR TANI = '';
```

**影響範囲**:
- 既存データの移行が必要
- 関連するSQL文の更新が必要
- データアクセス層の修正が必要

---

### 2. RegisterSeikyuMasterForm.vb 修正計画

#### 2.1 要件1: 自動計算機能の実装

**目的**: 単価×数量による金額の自動計算

**実装箇所**: `UcDgv_CellEndEdit`イベントハンドラー

**実装内容**:
```vb
Private Sub UcDgv_CellEndEdit(sender As Object, e As DataGridViewCellEventArgs) Handles UcDgv.DgvCellEndEdit
    Dim customDataGridView As CustomDataGridView = CType(sender, CustomDataGridView)
    Dim dataGridViewColumn As DataGridViewColumn = customDataGridView.Columns(e.ColumnIndex)
    Dim dataGridViewRow As DataGridViewRow = customDataGridView.Rows(e.RowIndex)
    
    ' 既存の処理...
    
    ' 自動計算機能（要件1）
    If "TANKA".Equals(dataGridViewColumn.Name) Or "SURYO".Equals(dataGridViewColumn.Name) Then
        CalculateKingaku(dataGridViewRow)
    End If
    
    ' 入力促進機能（要件2）
    ValidateInputFields(dataGridViewRow)
    
    ' 単位必須チェック（要件5）
    If "TANI".Equals(dataGridViewColumn.Name) Then
        ValidateTaniField(dataGridViewRow)
    End If
End Sub

Private Sub CalculateKingaku(row As DataGridViewRow)
    Try
        Dim tanka As Decimal = 0
        Dim suryo As Decimal = 0
        
        If Not IsDBNull(row.Cells("TANKA").Value) AndAlso Not String.IsNullOrEmpty(row.Cells("TANKA").Value?.ToString()) Then
            Decimal.TryParse(row.Cells("TANKA").Value.ToString(), tanka)
        End If
        
        If Not IsDBNull(row.Cells("SURYO").Value) AndAlso Not String.IsNullOrEmpty(row.Cells("SURYO").Value?.ToString()) Then
            Decimal.TryParse(row.Cells("SURYO").Value.ToString(), suryo)
        End If
        
        Dim kingaku As Decimal = tanka * suryo
        row.Cells("KINGAKU").Value = kingaku
        
        ' 合計金額の再計算
        RecalculateTotal()
        
    Catch ex As Exception
        OutputLog.WriteLine("CalculateKingaku Error: {0}", New String() {ex.Message})
    End Try
End Sub
```

#### 2.2 要件2: 入力促進機能の実装

**目的**: 単価、数量未入力時の背景色変更による入力促進

**実装内容**:
```vb
Private Sub ValidateInputFields(row As DataGridViewRow)
    ' 単価フィールドの検証
    If IsDBNull(row.Cells("TANKA").Value) OrElse String.IsNullOrEmpty(row.Cells("TANKA").Value?.ToString()) Then
        row.Cells("TANKA").Style.BackColor = Color.LightYellow
    Else
        row.Cells("TANKA").Style.BackColor = Me._bkcolor_normal
    End If
    
    ' 数量フィールドの検証
    If IsDBNull(row.Cells("SURYO").Value) OrElse String.IsNullOrEmpty(row.Cells("SURYO").Value?.ToString()) Then
        row.Cells("SURYO").Style.BackColor = Color.LightYellow
    Else
        row.Cells("SURYO").Style.BackColor = Me._bkcolor_normal
    End If
End Sub
```

#### 2.3 要件3: 金額フィールド保護の実装

**目的**: 金額フィールドの手入力禁止

**実装箇所**: グリッド初期化処理

**実装内容**:
```vb
Private Sub InitializeGridColumns()
    ' 既存の初期化処理...
    
    ' 金額列を読み取り専用に設定（要件3）
    If Me._gridViewInfo.dataInfo.ItemDetail.ContainsKey("KINGAKU") Then
        Dim kingakuColumn As DataGridViewColumn = Me.UcDgv.CustDgv.Columns("KINGAKU")
        If kingakuColumn IsNot Nothing Then
            kingakuColumn.ReadOnly = True
            kingakuColumn.DefaultCellStyle.BackColor = Me._bkcolor_readonly
        End If
    End If
End Sub
```

#### 2.4 要件4: 単位項目追加の実装

**目的**: 単位フィールドの追加とコンボボックス設定

**実装内容**:
```vb
Private Sub SetupTaniColumn()
    If Me._gridViewInfo.dataInfo.ItemDetail.ContainsKey("TANI") Then
        Dim taniColumn As DataGridViewComboBoxColumn = New DataGridViewComboBoxColumn()
        taniColumn.Name = "TANI"
        taniColumn.HeaderText = "単位"
        taniColumn.DataPropertyName = "TANI"
        taniColumn.Items.AddRange(New String() {"個", "本", "台", "式", "kg", "m", "㎡", "その他"})
        taniColumn.DefaultCellStyle.BackColor = Color.LightGoldenrodYellow
        
        ' 既存の列を置き換え
        Dim existingIndex As Integer = Me.UcDgv.CustDgv.Columns("TANI").Index
        Me.UcDgv.CustDgv.Columns.RemoveAt(existingIndex)
        Me.UcDgv.CustDgv.Columns.Insert(existingIndex, taniColumn)
    End If
End Sub
```

#### 2.5 要件5: 単位入力必須化の実装

**目的**: 単位フィールドの必須入力チェック

**実装内容**:
```vb
Private Sub ValidateTaniField(row As DataGridViewRow)
    row.Cells("TANI").ErrorText = Nothing
    
    If IsDBNull(row.Cells("TANI").Value) OrElse String.IsNullOrEmpty(row.Cells("TANI").Value?.ToString()) Then
        row.Cells("TANI").ErrorText = "単位は必須入力です"
        row.Cells("TANI").Style.BackColor = Color.LightPink
    Else
        row.Cells("TANI").Style.BackColor = Me._bkcolor_normal
    End If
End Sub
```

#### 2.6 SQL文の更新

**目的**: 新規TANIフィールドに対応したSQL文の更新

**実装内容**:
```vb
' BASESQL の更新
Me.BASESQL = "SELECT '' AS KUBUN, M.UCHIWAKE, M.SURYO, M.TANI, M.TANKA, M.KINGAKU, M.KAZEI_KBN, M.MEISAI_UMU, M.SAKI_CD, M.TEKIYO, M.SAKU_KBN FROM Ukeharai.T_SEIKYUM AS M LEFT JOIN Ukeharai.T_SEIKYU AS H ON H.TORI_CD = M.TORI_CD AND H.SEIKYU_YYYYMM = M.SEIKYU_YYYYMM "

' INSERT/UPDATE SQL の更新
Private Function GenerateUpdateSQL() As String
    Return "UPDATE T_SEIKYU SET " & _
           "TANKA = @Tanka, " & _
           "SURYO = @Suryo, " & _
           "KINGAKU = @Kingaku, " & _
           "TANI = @Tani, " & _
           "KAZEI_KBN = @KazeiKbn, " & _
           "MEISAI_UMU = @MeisaiUmu, " & _
           "SAKI_CD = @SakiCd, " & _
           "TEKIYO = @Tekiyo " & _
           "WHERE TORI_CD = @ToriCd AND SEIKYU_YYYYMM = @SeikyuYYYYMM AND SEIKYU_SQNO = @SeikyuSqno"
End Function
```

---

### 3. OutPutSeikyuForm.vb 修正計画

#### 3.1 データソース修正

**目的**: 新規TANIフィールドに対応したデータ取得

**実装内容**:
```vb
Private Function GetSeikyuData() As String
    Dim sql As String = "SELECT " & _
                       "S.TORI_CD, S.SEIKYU_YYYYMM, S.SEIKYU_TYPE, S.SEIKYU_NO, " & _
                       "S.KAZEI, S.HIKAZEI, S.SYOHIZEI, S.GOUKEI, " & _
                       "M.SEIKYU_SQNO, M.SAKU_KBN, M.UCHIWAKE, M.SURYO, M.TANI, " & _
                       "M.TANKA, M.KINGAKU, M.MEISAI_UMU, M.SAKI_CD, M.TEKIYO, " & _
                       "T.TORI_NAME " & _
                       "FROM Ukeharai.T_SEIKYU AS S " & _
                       "LEFT JOIN Ukeharai.T_SEIKYUM AS M ON S.TORI_CD = M.TORI_CD AND S.SEIKYU_YYYYMM = M.SEIKYU_YYYYMM " & _
                       "LEFT JOIN Ukeharai.M_TORI AS T ON S.TORI_CD = T.TORI_CD " & _
                       "WHERE S.SEIKYU_YYYYMM = @SeikyuYYYYMM"
    
    Return sql
End Function
```

---

### 4. OutputSeikyuReportS.rdlc 修正計画

#### 4.1 要件6-8: 列分離表示とフォーマット変更

**目的**: 数量と単位の列分離、縦線追加、列幅最適化

**現在の構造**:
```xml
<!-- 現在: 数量列（結合表示） -->
<TablixColumn>
  <Width>2.3175cm</Width>
</TablixColumn>
```

**修正後の構造**:
```xml
<!-- 修正後: 数量列（分離表示） -->
<TablixColumn>
  <Width>3.0cm</Width>
</TablixColumn>
<!-- 修正後: 単位列（新規追加） -->
<TablixColumn>
  <Width>0.8cm</Width>
</TablixColumn>
```

#### 4.2 数量表示部分の修正

**現在の実装**:
```xml
<Textbox Name="SURYO">
  <Value>=Format(Fields!SURYO.Value,"#,###") &amp; Fields!TANI.Value</Value>
</Textbox>
```

**修正後の実装**:
```xml
<!-- 数量列 -->
<Textbox Name="SURYO">
  <Value>=Format(Fields!SURYO.Value,"#,###")</Value>
  <Style>
    <Border>
      <Right>
        <Style>Solid</Style>
        <Width>1pt</Width>
      </Right>
    </Border>
    <TextAlign>Right</TextAlign>
    <PaddingRight>2pt</PaddingRight>
  </Style>
</Textbox>

<!-- 単位列 -->
<Textbox Name="TANI">
  <Value>=Fields!TANI.Value</Value>
  <Style>
    <TextAlign>Center</TextAlign>
    <FontSize>9pt</FontSize>
  </Style>
</Textbox>
```

#### 4.3 ヘッダー部分の修正

**修正内容**:
```xml
<!-- 数量ヘッダー -->
<Textbox Name="Textbox12">
  <Value>数量</Value>
  <Style>
    <Border>
      <Style>Solid</Style>
      <Width>0.5pt</Width>
      <Right>
        <Style>Solid</Style>
        <Width>1pt</Width>
      </Right>
    </Border>
    <TextAlign>Center</TextAlign>
  </Style>
</Textbox>

<!-- 単位ヘッダー（新規追加） -->
<Textbox Name="TextboxTaniHeader">
  <Value>単位</Value>
  <Style>
    <Border>
      <Style>Solid</Style>
      <Width>0.5pt</Width>
    </Border>
    <TextAlign>Center</TextAlign>
  </Style>
</Textbox>
```

---

### 5. SqlDataBase.vb 活用計画

#### 5.1 トランザクション処理

**目的**: データベース変更時の整合性確保

**実装パターン**:
```vb
Using sqlDb As New SqlDataBase(Me._conf.xmlConfData.xDataBase)
    If sqlDb.BeginTransaction() Then
        Try
            ' 複数のSQL実行
            sqlDb.execSql(updateSeikyuSQL)
            sqlDb.execSql(updateSeikyumSQL)
            
            If sqlDb.CommitTransact() Then
                ' 成功処理
            Else
                ' コミット失敗処理
            End If
        Catch ex As Exception
            sqlDb.RollbackTransact()
            Throw ex
        End Try
    End If
End Using
```

---

### 6. バリデーション強化計画

#### 6.1 CustomDataGridView 活用

**目的**: 既存のバリデーション機能を活用した入力チェック強化

**実装内容**:
```vb
Private Function ValidateAllRows() As Boolean
    Dim hasError As Boolean = False
    
    For Each row As DataGridViewRow In Me.UcDgv.CustDgv.Rows
        If Not row.IsNewRow Then
            ' 単価チェック
            If IsDBNull(row.Cells("TANKA").Value) Then
                row.Cells("TANKA").ErrorText = "単価を入力してください"
                hasError = True
            End If
            
            ' 数量チェック
            If IsDBNull(row.Cells("SURYO").Value) Then
                row.Cells("SURYO").ErrorText = "数量を入力してください"
                hasError = True
            End If
            
            ' 単位チェック（要件5）
            If IsDBNull(row.Cells("TANI").Value) Then
                row.Cells("TANI").ErrorText = "単位を選択してください"
                hasError = True
            End If
        End If
    Next
    
    Return Not hasError
End Function
```

---

## 📅 実装フェーズ計画

### Phase 1: データベース準備（1日）
1. T_SEIKYUテーブルのTANIフィールド追加
2. 制約とインデックスの作成
3. 既存データの初期値設定
4. データベース変更の動作確認

### Phase 2: UI基本機能実装（3日）
1. RegisterSeikyuMasterForm.vb の修正
   - 自動計算機能（要件1）
   - 入力促進機能（要件2）
   - 金額フィールド保護（要件3）
   - 単位項目追加（要件4）
   - 単位入力必須化（要件5）
2. 単体テストの実施

### Phase 3: レポート機能実装（2日）
1. OutputSeikyuReportS.rdlc の修正
   - 列分離表示（要件6）
   - RDLC修正（要件7）
   - 列幅最適化（要件8）
2. OutPutSeikyuForm.vb の修正
3. レポート出力テストの実施

### Phase 4: 統合テスト（2日）
1. 全機能の統合テスト
2. 既存機能への影響確認
3. 性能テストの実施
4. バグ修正

### Phase 5: ユーザー受入テスト（1日）
1. ユーザーによる動作確認
2. フィードバック対応
3. 最終調整

---

## ⚠️ リスク分析と対策

### 技術的リスク

#### リスク1: データベース移行時の整合性問題
**対策**: 
- 移行前の完全バックアップ取得
- 段階的移行の実施
- ロールバック手順の準備

#### リスク2: 既存機能への影響
**対策**:
- 広範囲な回帰テストの実施
- 既存SQL文の動作確認
- 段階的リリースの検討

#### リスク3: RDLC修正による表示崩れ
**対策**:
- 複数の印刷環境でのテスト
- PDF出力での確認
- 旧フォーマットとの並行運用期間設定

### 運用リスク

#### リスク4: ユーザーの操作習慣変更
**対策**:
- 事前のユーザー教育実施
- 操作マニュアルの更新
- 段階的な機能公開

#### リスク5: データ入力ミスの増加
**対策**:
- 入力チェック機能の強化
- エラーメッセージの分かりやすさ向上
- 入力支援機能の充実

---

## 🧪 テスト計画

### 単体テスト
- 自動計算ロジックのテスト
- バリデーション機能のテスト
- データベースアクセス機能のテスト

### 統合テスト
- 画面間の連携テスト
- データベース整合性テスト
- レポート出力テスト

### システムテスト
- 業務フロー全体のテスト
- 性能テスト
- セキュリティテスト

### ユーザー受入テスト
- 実際の業務データでのテスト
- ユーザビリティテスト
- 運用手順の確認

---

## 📊 期待効果

### 業務効率向上
- 手動計算ミスの削減: 95%削減目標
- 入力時間の短縮: 30%短縮目標
- 請求書確認時間の短縮: 20%短縮目標

### システム品質向上
- データ整合性の向上
- 保守性の向上
- 拡張性の向上

### ユーザー満足度向上
- 操作性の向上
- エラー発生率の低下
- 業務負荷の軽減

---

## 📝 実装時の注意事項

### コーディング規約
- 既存のコーディングスタイルに準拠
- 適切なエラーハンドリングの実装
- ログ出力の統一

### パフォーマンス考慮事項
- データベースアクセスの最適化
- メモリ使用量の監視
- レスポンス時間の測定

### セキュリティ考慮事項
- SQLインジェクション対策
- 入力値の適切な検証
- ログ情報の機密性確保

---

## 🔄 保守・運用計画

### 定期メンテナンス
- データベースインデックスの最適化
- ログファイルの管理
- 性能監視

### 障害対応
- 障害発生時の対応手順
- データ復旧手順
- 緊急連絡体制

### 機能拡張
- 新規単位の追加手順
- レポートフォーマットの変更手順
- 計算ロジックの変更手順

---

## 📚 関連資料

### 設計書
- 井関物流 受払管理システム基本設計書_20250606_2.md
- 受払システム_画面定義書(17.請求マスター登録)_20250606_2.md
- 受払システム_画面定義書(62.請求書出力)_20250606_2.md
- 受払管理システム_DB論理設計書_20250606_2.md

### 分析資料
- 新規改修要件詳細分析.md
- 設計書改修計画書_ver2.md

### テスト資料
- 単体テスト仕様書（作成予定）
- 統合テスト仕様書（作成予定）
- ユーザー受入テスト仕様書（作成予定）

---

**作成者**: Devin AI  
**承認者**: 井関物流 システム担当者  
**最終更新**: 2025年6月6日
